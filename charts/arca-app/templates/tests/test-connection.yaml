apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "arca-app.fullname" . }}-test-connection"
  labels:
    {{- include "arca-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "arca-app.fullname" . }}:{{ .Values.port }}']
  restartPolicy: Never
